import Logo from'../components/Logo'
export default function ConnectRazorpayPage({user,credentials,setCredentials,onConnect,notice}){return <main className="connect"><header><Logo/><span>{user.email}</span></header><section><p>SECURE CONNECTION</p><h1>Connect Razorpay</h1><p>Test credentials are Fernet-encrypted before storage.</p><form onSubmit={onConnect}><input required placeholder="Test Key ID (rzp_test_…)" value={credentials.key_id} onChange={e=>setCredentials({...credentials,key_id:e.target.value})}/><input required type="password" placeholder="Test Key Secret" value={credentials.key_secret} onChange={e=>setCredentials({...credentials,key_secret:e.target.value})}/><button className="primary">Connect securely →</button></form><small>{notice}</small></section></main>}
