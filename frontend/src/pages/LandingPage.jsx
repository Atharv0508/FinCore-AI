import {GoogleLogin}from'@react-oauth/google';import Logo from'../components/Logo'
export default function LandingPage({showLogin,setShowLogin,onGoogleLogin,notice}){return <main className="landing"><header><Logo/><button onClick={()=>setShowLogin(true)}>Sign in</button></header><section><p>FINANCE RECONCILIATION, REIMAGINED</p><h1>Every transaction.<br/><em>Perfectly reconciled.</em></h1><article>FinCore AI connects your Razorpay data, applies deterministic matching, and makes exceptions easy to resolve.</article><button className="primary" onClick={()=>setShowLogin(true)}>Get started →</button></section>{showLogin&&<div className="modal"><div><button onClick={()=>setShowLogin(false)}>×</button><Logo/><h2>Welcome to FinCore</h2><p>Sign in to create your workspace.</p><GoogleLogin onSuccess={onGoogleLogin}/><small>{notice}</small></div></div>}</main>}
